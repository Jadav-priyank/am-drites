"use client";

import ProductHero from "./ProductHero";

/**
 * Hero — thin wrapper that renders the premium 3D ProductHero section.
 * Replace this file's content if you want to revert to the original layout.
 */
export default function Hero({ scrollTo }) {
  return <ProductHero scrollTo={scrollTo} />;
}
